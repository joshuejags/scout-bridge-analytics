# Database
MONGODB_URI=mongodb://localhost:27017/scout-bridge-analytics
MONGODB_USER=
MONGODB_PASSWORD=

# Server
PORT=5000
NODE_ENV=development

# JWT — generate a real value with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here

# File Upload
MAX_FILE_SIZE=500000000
UPLOAD_DIR=./uploads

# Computer Vision
PYTHON_ENV_PATH=./venv
YOLO_MODEL=yolov8n.pt
OPENPOSE_MODEL_PATH=./models/openpose

# Frontend
REACT_APP_API_URL=http://localhost:5000/api
# Used to build password-reset / email-verification links sent to users
CLIENT_URL=http://localhost:3000

# Email (password reset + verification). Leave unset to log emails to the
# server console instead of sending them (fine for local dev; not fine for
# real users). Any SMTP provider works — SES, SendGrid, Postmark, etc.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Scout Bridge Analytics <no-reply@yourdomain.com>
