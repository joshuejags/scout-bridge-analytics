{
  "name": "scout-bridge-analytics",
  "version": "1.0.0",
  "description": "Sports analytics platform for analyzing player performance from match highlights",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run start --prefix client\"",
    "build": "npm run build --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run start --prefix client"
    ,"analysis-daemon": "node server/scripts/analysisDaemon.js"
  },
  "keywords": [
    "sports",
    "analytics",
    "computer-vision",
    "YOLO",
    "pose-estimation",
    "player-tracking"
  ],
  "author": "Adeolu Shobowale",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
  ,"dependencies": {
    "uuid": "^9.0.0"
  }
}
